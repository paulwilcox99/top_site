# Top-Level Site Aggregator Configuration
# =========================================

# Deployment mode: 'file' for file:// protocol, 'http' for web server
deployment_mode: 'http'

# Collection definitions
collections:
  - name: "Books"
    emoji: "📚"
    db_path: "../book-manager/books.db"
    site_path_file: "../book-manager/site/index.html"
    site_path_http: "https://pauls-books.vercel.app"
    accent_color: "#8b2942"
    db_table: "books"

  - name: "Albums"
    emoji: "🎵"
    db_path: "../albums/albums.db"
    site_path_file: "../albums/site/index.html"
    site_path_http: "https://pauls-albums.vercel.app"
    accent_color: "#e91e63"
    db_table: "albums"

  - name: "Broadway Shows"
    emoji: "🎭"
    db_path: "../broadway-shows/shows.db"
    site_path_file: "../broadway-shows/site/index.html"
    site_path_http: "https://pauls-broadway.vercel.app"
    accent_color: "#c41e3a"
    db_table: "shows"

# Site configuration
site:
  title: "Paul's Collections"
  subtitle: "Books, Music & Theater"
  output_dir: "site"
  show_recent_items: true
  recent_items_count: 5
